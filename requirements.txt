# ВНИМАНИЕ: это ещё не lock-файл — здесь диапазоны, а не точные версии.
# Список прямых зависимостей переехал в requirements.in; чтобы получить
# воспроизводимую сборку, соберите этот файл из него (нужна сеть):
#
#     uv pip compile requirements.in -o requirements.txt
#
# После этого здесь появятся точные версии и косвенные зависимости, и
# образ перестанет зависеть от дня сборки. Dockerfile и CI уже читают
# именно этот файл, менять их не нужно.

Django>=6.1,<6.2
psycopg[binary]>=3.2
openpyxl>=3.1        # разбор BOM-файлов Excel
django-crispy-forms>=2.3
Pillow>=10.4         # изображения плат: проверка при загрузке и уменьшение

# продакшен (используются в Docker-образе):
gunicorn>=23.0       # рабочий сервер
whitenoise>=6.7      # раздача собранной статики из самого приложения
redis>=5.0           # общий кэш, если задан DJANGO_CACHE_URL
